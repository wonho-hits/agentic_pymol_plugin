[pytest]
# Use importlib mode so pytest does not walk up looking for parent packages —
# the plugin root has __init__.py because PyMOL requires it, but importing it
# outside PyMOL fails on ``from pymol import cmd``.
addopts = --import-mode=importlib
testpaths = tests
