"""Plugin-side (PyMOL Python 3.10) components.

This package intentionally has minimal dependencies — only the standard
library plus the PyMOL runtime. All heavy agent / LangChain dependencies
live in the out-of-process ``agent`` uv project.
"""
